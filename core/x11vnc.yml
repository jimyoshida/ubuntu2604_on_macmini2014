---
- name: Create virtual framebuffer (Xvfb) and serve it over VNC (x11vnc)
  hosts: localhost
  connection: local
  gather_facts: yes

  vars:
    vnc_password: "{{ lookup('env', 'VNC_PASSWORD') | default('') }}"
    current_user: "{{ lookup('env', 'USER') }}"
    vnc_dir: "/home/{{ current_user }}/.vnc"
    systemd_user_dir: "/home/{{ current_user }}/.config/systemd/user"

  tasks:
    - name: 1. Check required variables
      assert:
        that:
          - vnc_password != ""
        fail_msg: "VNC_PASSWORD must be set"

    - name: 2. Install x11vnc and required packages
      become: yes
      apt:
        name:
          - x11vnc
          - x11-xserver-utils
          - x11-apps
          - xvfb
          - openbox
          - xauth
        state: present
        update_cache: yes

    - name: 3. Create VNC directory
      file:
        path: "{{ vnc_dir }}"
        state: directory
        mode: '0700'

    - name: 4. Create VNC password file
      shell: "x11vnc -storepasswd {{ vnc_password }} {{ vnc_dir }}/passwd"
      args:
        creates: "{{ vnc_dir }}/passwd"

    - name: 5. Set permissions for VNC password file
      file:
        path: "{{ vnc_dir }}/passwd"
        mode: '0600'

    - name: 6. Create VNC startup script
      copy:
        dest: "/home/{{ current_user }}/.vnc/start-x11vnc.sh"
        mode: '0755'
        content: |
          #!/bin/bash

          DISPLAY_NUM=1
          DISPLAY=":${DISPLAY_NUM}"
          SOCKET="/tmp/.X11-unix/X${DISPLAY_NUM}"
          AUTH_FILE="${HOME}/.vnc/Xvfb-auth"

          # Start Xvfb if display socket is absent
          if [ ! -e "$SOCKET" ]; then
              rm -f "$AUTH_FILE"
              cookie=$(head -c 16 /dev/urandom | od -An -tx1 | tr -d ' \n')
              /usr/bin/xauth -f "$AUTH_FILE" add "$DISPLAY" . "$cookie"

              /usr/bin/Xvfb "$DISPLAY" -screen 0 1920x1080x24 -auth "$AUTH_FILE" &

              for i in $(seq 1 20); do
                  [ -e "$SOCKET" ] && break
                  sleep 0.5
              done

              if [ ! -e "$SOCKET" ]; then
                  echo "Error: Xvfb failed to start on $DISPLAY" >&2
                  exit 1
              fi
          fi

          export DISPLAY XAUTHORITY="${AUTH_FILE}"
          export DBUS_SESSION_BUS_ADDRESS="unix:path=/run/user/$(id -u)/bus"

          systemctl --user import-environment DISPLAY XAUTHORITY DBUS_SESSION_BUS_ADDRESS

          DISPLAY="${DISPLAY}" XAUTHORITY="${AUTH_FILE}" /usr/bin/openbox &>/dev/null &
          disown

          exec /usr/bin/x11vnc \
            -display "${DISPLAY}" \
            -auth "${AUTH_FILE}" \
            -forever \
            -loop \
            -noxdamage \
            -repeat \
            -rfbauth "${HOME}/.vnc/passwd" \
            -rfbport 5901 \
            -shared \
            -xkb

    - name: 7. Create systemd user directory
      file:
        path: "{{ systemd_user_dir }}"
        state: directory
        mode: '0755'

    - name: 8. Create systemd user service file for x11vnc
      copy:
        dest: "{{ systemd_user_dir }}/x11vnc.service"
        mode: '0644'
        content: |
          [Unit]
          Description=x11vnc VNC server (Xvfb virtual display :1)
          After=network.target

          [Service]
          Type=simple
          ExecStart=%h/.vnc/start-x11vnc.sh
          Restart=on-failure
          RestartSec=3

          [Install]
          WantedBy=default.target

    - name: 9. Add DISPLAY and XAUTHORITY to .bashrc
      blockinfile:
        path: "/home/{{ current_user }}/.bashrc"
        marker: "# {mark} ANSIBLE MANAGED BLOCK: openbox-display"
        block: |
          export DISPLAY=:1
          export XAUTHORITY="${HOME}/.vnc/Xvfb-auth"
        create: yes

    - name: 10. Enable systemd lingering
      become: yes
      command: loginctl enable-linger {{ current_user }}
      changed_when: false

    - name: 11. Reload systemd user daemon
      systemd:
        daemon_reload: yes
        scope: user

    - name: 12. Enable and start x11vnc user service
      systemd:
        name: x11vnc
        scope: user
        enabled: yes
        state: started

    - name: 13. Print instructions
      debug:
        msg:
          - "Virtual framebuffer (Xvfb) created and served over VNC (x11vnc) — setup complete."
          - ""
          - "Running as user: {{ current_user }}"
          - "VNC password stored in: {{ vnc_dir }}/passwd"
          - "Startup script: {{ vnc_dir }}/start-x11vnc.sh"
          - "Service file: {{ systemd_user_dir }}/x11vnc.service"
          - "VNC port: 5901"
          - ""
          - "Service management (user-level daemon):"
          - "  Start:   systemctl --user start x11vnc"
          - "  Stop:    systemctl --user stop x11vnc"
          - "  Restart: systemctl --user restart x11vnc"
          - "  Status:  systemctl --user status x11vnc"
          - "  Logs:    journalctl --user -u x11vnc -f"
          - ""
          - "Security notes:"
          - "  - Virtual display :1 is created by Xvfb (no physical screen required)"
          - "  - VNC password file is restricted to user access only (mode 0600)"
          - "  - Service runs with your user permissions (not root)"
          - "  - Systemd lingering enabled (service persists after logout)"
          - ""
          - "To change VNC password:"
          - "  x11vnc -storepasswd {{ vnc_dir }}/passwd"
          - "  systemctl --user restart x11vnc"
          - ""
          - "To test manually (without systemd):"
          - "  {{ vnc_dir }}/start-x11vnc.sh"
          - ""
          - "To connect:"
          - "  Use any VNC client to connect to <this-machine-ip>:5901"
