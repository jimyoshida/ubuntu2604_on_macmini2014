#!/bin/bash

# n8n.yml
export N8N_PORT=        # Port for n8n UI (default: 5678)

# claude-code.yml / antigravity-cli.yml
export HTTPS_PROXY=     # HTTPS proxy URL — optional; leave empty if not behind a proxy
